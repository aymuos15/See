# Viewer

A TUI file viewer built with Ratatui.

## Build

```bash
cargo build --release
```

## Run

```bash
cargo run [FILE_PATH]
```

## Controls

- `q` - Quit
- `Shift+H` / `Shift+L` - Shrink/grow file list pane
